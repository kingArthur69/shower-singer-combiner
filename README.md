# shower-singer-combiner

Combiner REST API for Shower Singer.

Entrypoint is "/audio".

Accepts two files "song" and "vocals" via POST request form-data.

Returns combined user vocals with song accompaniment as a file.
